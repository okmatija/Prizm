#load "imgui_impl_sdl.jai"; // SDL platform backend
Simp :: #import "Simp";

main :: () {

    window : *SDL_Window;

    Simp.update_window(get_window_type(window)); // Comment this line to allow compilation to complete!
}
