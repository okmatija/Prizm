main :: () {
    // With the following line in main() the compilation hangs.
    // You can also try commenting this to see a Panic which looks incorrect
    // Simp.update_window(get_window_type(null));
}

ImGui :: #import "ImGui";
Simp :: #import "Simp";